{
  "name": "triton-piano-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check dist/app.js && node --check dist/content.js && node --check dist/legal.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
